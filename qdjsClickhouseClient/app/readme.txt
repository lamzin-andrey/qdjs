Текущяя проблема.
Почему не взлетела построчная подсветка.


2 Что делает this.selection.calc(); в текущей V2 подсветке?
  - записывает данные о позиции курсора в rules
  надо будет решить проблему с выделением, сейчас выделяет в каждой строке.

5 Что делать с многострочными комментариями и многострочными строками.
   Тут надо что-то сделать, чтобы предавать is функциям весь контент и 
	позицию в этом контенте.

1* Еще раз внимательно изучить, что у меня за фича со сдвоенными переносами.
   Задваиваем любые идущие подряд или всё таки самый последний?
     - самый последний (вроде нет проблем)
   

3* Удалить бред про есть / нет буква. Если есть позиция и строка, красим текущую строку, иначе	*
    рендерим строки. (тут всё сделал, вроде ок).

4* Почему зеленый курсор при переносе в конец строки оказывается визуально на новой строке?
 - Пофиксил, надо в конец каждой div строки добавлять <i>&nbsp;</i>
 
